name: Build

on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3  # Atualizar para a versão mais recente
        with:
          fetch-depth: 0

      - name: Install dependencies
        run: yarn install

      - name: Run tests and collect coverage
        run: yarn test --coverage

      - name: List coverage directory  # Verificar o conteúdo do diretório de cobertura
        run: ls -R coverage

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v4
        with:
          sonar-project-key: "Costanza22_front-cloud"
          sonar-host-url: "https://sonarcloud.io"
          sonar-login: ${{ secrets.SONAR_TOKEN }}
          sonar.organization: "Costanza22"  # Definido corretamente aqui
          sonar.javascript.lcov.reportPaths: "coverage/lcov-report/lcov-report.json"  # Caminho correto do arquivo lcov

